<%= form_for(@location_hour) do |f| %>
  <% if @location_hour.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@location_hour.errors.count, "error") %> prohibited this location_hour from being saved:</h2>

      <ul>
      <% @location_hour.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.label :location_id %>
    <%= f.text_field :location_id %>
    <%= f.label :day_of_week %>
    <%= f.select :day_of_week, options_for_select(%w[Sunday Monday Tuesday Wednesday Thursday Friday Saturday]) %>
    <%= f.label :open %>
    <%= time_select LocationHour, :open, {ampm: true, start_hour: 12} %>
    <%= f.label :close %>
    <%= time_select LocationHour, :close, {ampm: true, start_hour: 12}  %>
    <%= f.submit %>
  </div>
<% end %>
